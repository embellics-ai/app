# Example staging environment variables
# Copy to .env.staging and fill with real values for staging
NODE_ENV=staging
DATABASE_URL=your_staging_db_url
SMTP_HOST=your_staging_smtp_host
SMTP_PORT=587
SMTP_USER=your_staging_smtp_user
SMTP_PASS=your_staging_smtp_pass
SKIP_EMAIL=false
